# arkenea-backend
Steps:
1. Install dependencies using 'npm i'.
2. Run the project using 'npm start'
3. To load the meta data for the first time hit the POST API from postman or whatever you would like http://localhost:3000/loadCategories
4. Now, Run the front-end project using npm start and go to http://localhost:4200/ while keep running this server side.
